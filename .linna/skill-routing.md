# skill-routing.md — Lina 专业能力链路（自持执行）

> **初始定稿：2026-08-13（韩工指令补充：子 Agent 需要有自己的 skill 链路）**
> **用途：** 能力域 → skill/能力映射 → 入口触发 → 执行编排 → 产出/出口 → 依赖数据的完整路由。
> **自持：** 链路由我维护、由我执行；Nova 只做任务调度，不插手技能链路细节。
> **联动：** 能力登记见 `skills-index.md`（本文件引用其能力 key）；格式遵循 `nova/skill-format-standard.md`；缺能力时按该标准自建 skill 补位，并同步更新本文件 + skills-index.md。
> **覆盖：** 六部门制业务能力 6 域 + 工具能力 10 域。

---

## 链路通用约定

| 项 | 约定 |
|:---|:-----|
| 入口来源 | 韩工直接指令 / Nova 派工（sessions_send）/ 定时任务（scheduler）/ 实时触发（平台规则监测） |
| 编排总则 | 取依赖数据 → 校验来源（真实性红线）→ 执行 → 自检 → 出口 → 留痕（02_RUNTIME_INDEX_LOG + git push） |
| 出口路径 | `output/{部门}/`（成品，仅韩工主动要的）；`Lina/{部门}/`（台账/中间产物）；`~/NovaShared/OutPut/`（与 output 一致镜像） |
| 出口格式 | 按 TOOLS.md 格式标准：html（首选）/ pdf / csv（表单数据，韩工明确要求才 xlsx）/ docx / md（底层留存） |
| 法务前置 | 全部经营链路先过铁律六法律校验，P0-P3 分级 |
| 风控兜底 | 全部经营链路过五级熔断检查 |

> **跨 Agent 共享规则总索引：`~/NovaShared/协作区/Index/shared-rules-index.md`（Nova 维护，2026-08-13）——规则疑问先查总索引定位权威源，遵循单一权威源+引用模式，升级自动同步。**

---

## 一、六部门业务链路

### 1. 经营调研部（`market-intelligence`）

| 环节 | 内容 |
|:---|:-----|
| 能力引用 | skills-index.md → 经营调研部（待完善） |
| 入口触发 | 韩工/Nova 派发市场研究、商业分析、竞品监测、行业动态任务；周/月复盘需要行业背景时 |
| 执行编排 | ① web-research 抓取行业/竞品公开信息 → ② 来源核实（真实性红线）→ ③ 交叉比对运营数据 → ④ 出调研结论 → ⑤ 自检（结论先行、数据可追溯） |
| 产出/出口 | 调研简报 → `output/business/`（成品）；中间稿 → `Lina/其他/`；结论并入复盘报告 |
| 依赖数据 | `Lina/运营数据/`、网络公开信息（web-research） |
| 缺能力自建 | 市场监测体系未成型；需时按 skill-format-standard 补建（如 market-intelligence-analyzer）并登记 |

### 2. 法务合规部（`legal-compliance`）

| 环节 | 内容 |
|:---|:-----|
| 能力引用 | skills-index.md → 法务合规部（在用） |
| 入口触发 | 选品/上架/定价/广告/合同/售后/用工等业务动作触发铁律六风险扫描；韩工指令合同审查、纠纷研判、风险普法 |
| 执行编排 | ① 触发词识别（00_CORE_IRON_RULES 铁律六 6.1 表）→ ② 先查记忆（法规笔记）→ ③ 翻原文核实（法规文件/）→ ④ P0-P3 分级 → ⑤ 按格式输出风险提示 → ⑥ 无风险不报 |
| 产出/出口 | 风险提示（对话内，⚠️ P0-P3 格式）；合规报告/合同审查意见 → `output/{对应部门}/` |
| 依赖数据 | `Lina/法规文件/`（308部原文）、memory/kb-index/（学习笔记） |
| 缺能力自建 | 无（在用） |

### 3. 财务审计部（`finance-audit`）

| 环节 | 内容 |
|:---|:-----|
| 能力引用 | skills-index.md → 财务审计部（在用） |
| 入口触发 | 韩工指令记账/核算/台账/报税归档；每日2点自动复盘；每月1号月度优化 |
| 执行编排 | ① 取运营数据（订单/收支）→ ② 逐笔记账（进价/售价/运费/利润）→ ③ 日清月结核算 → ④ 生成台账 → ⑤ 财税合规校验（法务联动）→ ⑥ 归档 |
| 产出/出口 | 财税台账/财税合规报告/税前扣除清单/报税备查资料 → `output/finance/` |
| 依赖数据 | `Lina/运营数据/`、`Lina/商品资料/`（进价）、`Lina/供应商资料/`（采购价） |
| 缺能力自建 | 无（在用） |

### 4. 人事行政部（`hr-admin`）

| 环节 | 内容 |
|:---|:-----|
| 能力引用 | skills-index.md → 人事行政部（待完善） |
| 入口触发 | 韩工指令用工合规、薪酬制度、岗位权责、人事台账、行政制度 |
| 执行编排 | ① 查 `Lina/hr/` 底稿 → ② 法务合规校验（劳动法/劳动合同法/社保法）→ ③ 出制度/台账/范本 → ④ 归档 |
| 产出/出口 | 人事制度/用工台账/合规报告/合同范本 → `output/hr/` |
| 依赖数据 | `Lina/hr/`、`Lina/法规文件/`（劳动法系） |
| 缺能力自建 | 制度体系未成型；需时按标准补建（如 hr-admin-tracker）并登记 |

### 5. 商业运营部（`commerce-ops`）

| 环节 | 内容 |
|:---|:-----|
| 能力引用 | skills-index.md → 商业运营部（在用） |
| 入口触发 | 店铺运营/订单履约/供应链/库存/产品管理任务；实时触发（订单、库存事件） |
| 执行编排 | ① 取运营数据 + 平台规则 → ② 履约/库存/产品动作 → ③ 法务前置校验（铁律六）→ ④ 风控检查（五级熔断）→ ⑤ 台账更新 → ⑥ 出口 |
| 产出/出口 | 正式SOP/库存台账/订单报表 → `output/business/`；产品台账/上架SOP → `output/product/` |
| 依赖数据 | `Lina/运营数据/`、`Lina/商品资料/`、`Lina/供应商资料/`、`Lina/平台规则/` |
| 缺能力自建 | 无（在用） |

### 6. 风控品控部（`risk-control`）

| 环节 | 内容 |
|:---|:-----|
| 能力引用 | skills-index.md → 风控品控部（在用） |
| 入口触发 | 五级熔断触发（资金/库存/亏损/合规/账号）；质量溯源、售后风控任务；定时巡检 |
| 执行编排 | ① 风险信号识别（阈值比对，01 §五）→ ② 分级（五级+集群）→ ③ 自动止损动作 → ④ 法务联动（P0-P3）→ ⑤ 复盘留痕 |
| 产出/出口 | 风险清单/熔断报告 → `output/quality/`；月度/季度复盘 → `output/archive/` |
| 依赖数据 | `Lina/运营数据/`（订单/广告/退款）、`Lina/商品资料/`（质检）、`Lina/法规文件/` |
| 缺能力自建 | 无（在用） |

---

## 二、工具能力链路

### 7. 平台规则全天候监测（`platform-rule-monitor`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 实时抓取平台官方更新；每日2点合规全检 |
| 执行编排 | ① 抓取官方源 → ② 新旧对比识别变更 → ③ 分级处理（01 §四 SOP）→ ④ 适配动作 → ⑤ 留痕 |
| 产出/出口 | 规则变更简报 → `output/business/`；适配动作记录 |
| 依赖数据 | `Lina/平台规则/`、web-research |

### 8. 定时自动化任务体系（`scheduler`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 定时：每小时监控 / 每日2点复盘+合规全检 / 每日9点极简推送 / 每周一复盘 / 每月1号优化 / 实时触发 |
| 执行编排 | ① 到点触发 → ② 确定性子步骤走脚本（**每日9点早报：先跑 `scripts/cron/lina_morning_brief_prep.sh` 产出数据准备文件 `Lina/运营数据/早报数据准备_当日.md`，模型只 read 该文件后生成推送**，不自行查询；脚本化改造 2026-08-18 方法论 §三）→ ③ 按任务清单执行（01 §三）→ ④ 自检 → ⑤ 出口/推送 → ⑥ 留痕 |
| 脚本入口 | `scripts/cron/lina_morning_brief_prep.sh`（bash 直接跑，产出供只读）；其余 cron 任务脚本化逐个推进中 |
| 产出/出口 | 按任务类型落 `output/{部门}/`；每日9点极简推送（对话内） |
| 依赖数据 | 各业务链路产出 |

### 9. 多店铺矩阵合规运营（`multi-store-ops`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 扩店、新店入驻、多店风控事件 |
| 执行编排 | ① 平台开店限额核验（01 §10.2）→ ② 主体隔离检查 → ③ 风控关联六维判定 → ④ 集群熔断判断 → ⑤ 出口 |
| 产出/出口 | 多店合规方案 → `output/business/` |
| 依赖数据 | `Lina/平台规则/`、`Lina/运营数据/` |

### 10. 法规库学习与应用（`law-kb`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 新法规发布监测；业务触发法律扫描；定期深度学习 |
| 执行编排 | ① 官方源抓取（01 §9.2）→ ② 智能拆解学习（01 §9.4）→ ③ 更新实体库（memory/kb-index/）→ ④ 业务联动校验 |
| 产出/出口 | 学习笔记 → `Lina/学习笔记/`；合规联动结果进各业务链路 |
| 依赖数据 | `Lina/法规文件/`、memory/kb-index/ |

### 11. 文件与台账管理（`file-ledger-mgmt`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 任何读写文件动作前；输出交付时 |
| 执行编排 | ① 判定去向（韩工主动要 → output/；否则 Lina/）→ ② 命名规则（TOOLS.md：时间戳_版本_名称_批次）→ ③ 格式标准 → ④ 版本保留 → ⑤ 共享镜像同步 |
| 产出/出口 | 文件落位 + 同步记录 |
| 依赖数据 | TOOLS.md、铁律七（资料隔离） |

### 12. 企业知识库与记忆（`memory-kb`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 配置/供应商/商品数据变更；任务执行前查记忆 |
| 执行编排 | ① memory_search 检索 → ② 更新 MEMORY.md / memory/* → ③ 快照与索引维护 |
| 产出/出口 | 记忆更新记录 |
| 依赖数据 | memory/、knowledge/ |

### 13. 网络检索与外部核实（`web-research`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 调研/法规/平台规则/行业资料需要外部信息 |
| 执行编排 | ① web_search / web_fetch → ② 来源核实（真实性红线：可追溯）→ ③ 引用进链路 |
| 产出/出口 | 核实结果进各业务链路 |
| 依赖数据 | 外部公开源 |

### 14. 多智能体协作（`agent-collab`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 任务同步 Nova；跨 Agent 协作需求 |
| 执行编排 | ① 任务识别同步（sessions_send → main）→ ② 跨 Agent 请求（铁律八：不直接动他人文件）→ ③ 咬尾防御（铁律九：收到一次即止） |
| 产出/出口 | 协作记录 |
| 依赖数据 | 铁律八/九、AGENTS_ARCHITECTURE.md |

### 15. Git 变更管理（`git-sync`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 重要配置/规则/文件变更后 |
| 执行编排 | ① git add / commit → ② push（会话内手动，cron 已禁用）→ ③ 记录 |
| 产出/出口 | commit 记录 |
| 依赖数据 | TOOLS.md（Git 段落） |

### 16. 共享知识中枢（`knowledge-share`）

| 环节 | 内容 |
|:---|:-----|
| 入口触发 | 产生可复用经营合规沉淀（法规库摘要/平台规则/财务人事制度）；需要跨 Agent 共享素材时 |
| 执行编排 | ① 按「共享知识登记 SOP」复制到 Knowledge/business/ → ② 全局索引登记 → ③ 通知 Nova → ④ 检索共享域素材（README §四） |
| 产出/出口 | 共享域文件 + 全局索引条目 + 通知记录 |
| 依赖数据 | ~/NovaShared/协作区/README_共享知识中枢.md、协作区/Knowledge/business/、全局索引（material_index.py） |

---

## 三、共享知识登记 SOP（全局知识共享机制）

> 上位：`~/NovaShared/协作区/README_共享知识中枢.md`（必读）。2026-08-13 韩工定：整体一盘棋，各自维护各自负责的部分，Nova 统筹管理。
> 我的共享域：`~/NovaShared/协作区/Knowledge/business/`（经营合规域：法规库摘要/平台规则/财务人事制度）。

| 步骤 | 动作 |
|:---:|:-----|
| ① | 产生可复用沉淀（经营合规类知识；私有数据除外） |
| ② | 复制到 `~/NovaShared/协作区/Knowledge/business/{分类}/` |
| ③ | 向全局索引登记：`python3 ~/.openclaw/workspace/skills/material-index-maintainer/scripts/material_index.py add --name {文件名} --type {类型} --category {分类} --agent lina --domain business --share_level shared --location {绝对路径} --entity "{实体:方面,...}"` |
| ④ | sessions_send 通知 Nova（摘要：登记了什么） |

**检索入口（所有 Agent 找素材/参考的统一入口）：**

```bash
python3 ~/.openclaw/workspace/skills/material-index-maintainer/scripts/material_index.py search --kw {关键词} [--agent lina] [--domain business] [--share_level shared]
```

**边界：** 私有数据（供应商资料/商品资料/运营数据/配置）不共享；索引为单一权威，找素材先查索引，不靠记忆/转发（README §五）。
**生命周期维护：** 共享文件更新/删除/失效/登记错误/索引不一致，按 README §3.2 执行（更新/删除同步索引，谁登记谁负责，Nova 统筹仲裁）；登记契机 4 类见 README §3.1。

---

## 四、知识共享与共建处理（引用式）

> **共享/维护/共建机制遵循 `~/NovaShared/协作区/README_共享知识中枢.md`（v1.2，2026-08-18），以该文件为准；升级只改该文件，引用自动同步。** 规则性内容（登记契机 4 类/生命周期责任表/共建规则/边界）见 README §3.1/§3.2/§六/§五，本节不复制。本节仅保留：本地操作命令速查 + 我的共建申报。

### 4.1 操作命令速查（本地保留，操作时不依赖再读文档）

```bash
# 脚本路径
PY=~/.openclaw/workspace/skills/material-index-maintainer/scripts/material_index.py

# 登记（共享/共建）：
python3 $PY add --name {文件名} --type {类型} --category {分类} --agent lina --domain business --share_level shared [--co_build {主建},{共建}] --location {绝对路径} --entity "{实体:方面,...}"

# 检索（找素材/参考先查索引）：
python3 $PY search --kw {关键词} [--agent lina] [--domain business] [--share_level shared] [--format json]

# 更新（共享文件内容变更后同步）：
python3 $PY update --id {id} --field {字段} --value {新值}

# 删除（共享文件删除/废弃后同步）：
python3 $PY remove --id {id}
```

> 注：README §6.4 警告勿用 --co-build；脚本参数为 --co_build（下划线）。

### 4.2 共建申报清单（本地记录，申报 Nova 登记入 Index/co-build.md）

| 资料 | 类型 | 主建 | 共建 | 理由 |
|:---|:---|:---|:---|:---|
| 政策法规解读（308 部学习笔记） | 法规库摘要 | Lina | Nova/Ellie/Drama | 公共参考库，跨 Agent 引用（README §6.1 示例场景） |
| 经营合规配置与 SOP（01_BUSINESS_CONFIG） | 制度文档 | Lina | Nova | Nova 派工/调度需了解经营 SOP |
| 财税政策监测简报 | 财税合规 | Lina | Nova | 总工分析报告引用财税数据 |

---

## 自检清单（每次链路变更后执行）

| 项 | 标准 |
|:---|:-----|
| 覆盖 | 全部能力域：6 业务域 + 10 工具域，无遗漏 |
| 引用有效 | 能力 key 与 skills-index.md 一一对应，无失效引用 |
| 出口正确 | output/{部门}/ 与 TOOLS.md 目录一致 |
| 联动 | 能力变更时同步更新本文件 + skills-index.md + 02_RUNTIME_INDEX_LOG.md |

---

## 变更记录

| 日期 | 变更内容 | 定稿人 |
|:----|:--------|:------|
| 2026-08-13 | 链路通用约定新增引用：跨 Agent 共享规则总索引 shared-rules-index.md（Nova 维护，规则疑问先查总索引定位权威源） | 韩工指令 / Lina 执行 |
| 2026-08-13 | 专节四改引用式：删除复制内容（契机表/生命周期表/共建规则），保留命令速查 + 共建申报；引用 README v1.0（韩工定：单一权威源，升级自动同步） | 韩工指令 / Lina 执行 |
| 2026-08-13 | 新增专节「四、知识共享与共建处理」：共享处理/生命周期/共建规则/检索入口/共建申报清单（README §六 共建机制内化） | 韩工指令 / Lina 执行 |
| 2026-08-13 | SOP 补充生命周期维护引用：按 README §3.2 执行（更新/删除同步索引），登记契机见 §3.1 | 韩工指令 / Lina 执行 |
| 2026-08-13 | 新增「共享知识登记 SOP」+ 工具链路 #16 共享知识中枢（knowledge-share）（韩工定：多智能体知识共享机制，整体一盘棋，Nova 统筹） | 韩工指令 / Lina 执行 |
| 2026-08-13 | 初始定稿：15 条链路（6 业务域 + 9 工具域），自持执行约定 + 自检清单 | 韩工指令 / Lina 执行 |
