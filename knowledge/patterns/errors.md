# 🕳️ 错误模式库（从踩坑中学习）

> 记录每个踩过的坑：触发条件、损失、修复方案、预防机制。
> **只有记录下来的坑才是真正的教训。**

---

## 2026-08

| 日期 | 错误模式 | 触发条件 | 修复 | 预防 |
|:----|:--------|:--------|:----|:----|
| 08-22 | .md 学习笔记再次误同步共享 OutPut（复发 07-06） | legal-deep-study 产物每日批量学习_2026-08-22.md 按旧约定同步到 `NovaShared/OutPut/`，被 Nova 删除 | 全量修正：任务单/prep 脚本/数据准备 md 同步表述改为「md 仅本地留存，OutPut 只放交付文档 .html/.pdf/.csv/.docx；共享走 material_index 登记」 | 铁律·输出前自问：Hans 找我要了吗？+ md 类产出永不进 OutPut（grep 自查零容忍） |
| 08-12 | 共享路径架构变更未及时同步 | NovaShared/Lina/ 已清空停用，主存储改 workspace | 已更新 .linna/ 全部配置（MEMORY/TOOLS/00/02） | 架构变更后立即全文件 grep -rn 自查 |

## 2026-07

| 日期 | 错误模式 | 触发条件 | 修复 | 预防 |
|:----|:--------|:--------|:----|:----|
| 07-06 | 改名未通知Nova | 自身身份变更未通过 sessions_send 同步 | 补发身份对齐通知 | 任何身份/规则变更后即时同步 Nova |
| 07-06 | .md 文件误放到共享 OutPut | 法律学习笔记直接同步到 `NovaShared/Linna/OutPut/` | 已删除，移回本地 input | 铁律·输出前自问：Hans 找我要了吗？ |
| 07-06 | 内部文件路径未同步 config 变更 | 工作区改名 `workspace-linna→workspace-lina` 未自查 | 全面排查并更新所有硬编码路径 | 全局配置变更后立即全文件搜索自查 |
| 07-06 | NovaShared 路径过期 | 规则文件里写的共享路径仍是 `/mnt/hgfs/NovaShared/`，实际是 `ElliShared` | 已全域更新 | 路径变更后立即自查全部规则文件 |
| 07-06 | 铁律违规后果被定义得太轻 | 原来写"第一次批评第二次不可接受" | 已修正为铁律级：**只要犯错就是机制漏洞** | 铁律就是铁律，没有两次机会 |
